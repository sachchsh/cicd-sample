# - Airflow Env variables.
substitutions:
  _COMPOSER_REGION: asia-south1
  _COMPOSER_ENV_NAME: composer-airflow-2
  

  
# Access the id_github file from Secret Manager, and setup SSH
steps:
  - name: 'gcr.io/cloud-builders/git'
    secretEnv: ['SSH_KEY']
    entrypoint: 'bash'
    args:
    - -c
    - |
      echo "$$SSH_KEY" >> /root/.ssh/id_rsa
      chmod 400 /root/.ssh/id_rsa
      ssh-keyscan -t rsa github.com > known_hosts.github
      cp known_hosts.github /root/.ssh/known_hosts
# cat known_hosts.github 
    volumes:
    - name: 'ssh'
      path: /root/.ssh
  
# Clone the repository
  - name: 'gcr.io/cloud-builders/git'
    args:
    - clone
    - --recurse-submodules
    - git@github.com:Hasimk/cicd-sample.git
    - checkout version1.0
    volumes:
    - name: 'ssh'
      path: /root/.ssh
 # - Unit tests.
  # [START unit-test]
  - name: 'gcr.io/${PROJECT_ID}/quickstart-image:tag6'
    waitFor: ['quickstart-image']
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        pytest tests/unit
#         if [ "${_COMPOSER_ENV_NAME}" = "${_DEV_ENV_NAME}" ]
#         then
      
#         else
#           echo "***************************** unit-test *******************************"
#           echo "Environment '"$_COMPOSER_ENV_NAME"' does not require this step ... skipping"
#           echo "***************************************************************************"
#         fi
    id: "unit-test"
  # [END unit-test]

   

# # # [START composer_cicd_dagsync_yaml]

# # install dependencies
#   - name: python
#     entrypoint: pip
#     args: ["install", "-r", "utils/requirements.txt", "--user"]

# # run
#   - name: python
#     entrypoint: python
#     args: ["utils/add_dags_to_composer.py", "--dags_directory=${_DAGS_DIRECTORY}", "--dags_bucket=${_DAGS_BUCKET}"]

# [END composer_cicd_dagsync_yaml]
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
    - '-c'
    - |
      for dagfile in $(ls dags)
        do
        gcloud composer environments storage dags import \
        --environment $_COMPOSER_ENV_NAME \
        --location $_COMPOSER_REGION \
        --source dags/$$dagfile
        done
 # [Start Artifact registry]
  - name: 'gcr.io/cloud-builders/docker'
    args: [ 'build', '-t', '$_COMPOSER_REGION-docker.pkg.dev/$PROJECT_ID/quickstart-docker-repo/quickstart-image:tag6', '.' ]
images:
  - 'asia-south1-docker.pkg.dev/$PROJECT_ID/quickstart-docker-repo/quickstart-image:tag6' 
  
 ## [End Artifact registry]



   
availableSecrets:
    secretManager:
    - versionName: projects/591981386330/secrets/SSH_KEY/versions/latest
      env: 'SSH_KEY'
      
